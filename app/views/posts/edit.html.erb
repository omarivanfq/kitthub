<%= form_for @post do |f| %> 
  <%= f.label :body %>
  <%= f.text_field :body %>
  <br>
  <%= f.file_field :picture%>
  <br>
  <%= f.hidden_field :cat_id, value: @cat.id %>
  <%= f.submit 'Publish'%>
<% end %>
